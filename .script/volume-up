VOLUME=$( pactl list sinks | grep '^[[:space:]]Volume:' | head -n $(( $SINK + 1 )) | tail -n 1 | sed -e 's,.* \([0-9][0-9]*\)%.*,\1,')


if [ ${VOLUME} -le 145 ]; then
	pactl set-sink-volume @DEFAULT_SINK@ +7% && pkill -RTMIN+10 i3blocks
fi
